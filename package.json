{
  "name": "football-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && pnpm dev",
    "dev:api": "cd backend && uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload",
    "build": "cd frontend && pnpm install && pnpm build"
  },
  "packageManager": "pnpm@10.4.1"
}
