---
phase: 14-multi-model-evaluation-comparison
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/evaluation/vlm_comparison.py
  - backend/evaluation/tests/test_openai_vision.py
autonomous: true
requirements: [EVAL-03]
must_haves:
  truths:
    - "OpenAIVisionProvider.generate() sends base64 images via content parts"
    - "vlm_comparison.py --provider openai runs OpenAI vision evaluation"
    - "vlm_comparison.py --provider all iterates Gemini + OpenAI vision providers"
    - "Vision providers share a common interface with images: list[bytes] | None parameter"
  artifacts:
    - path: "backend/evaluation/vlm_comparison.py"
      provides: "OpenAIVisionProvider + multi-provider VLM comparison"
      contains: "class OpenAIVisionProvider"
    - path: "backend/evaluation/tests/test_openai_vision.py"
      provides: "Unit tests for OpenAIVisionProvider"
      contains: "test_generate_with_images"
  key_links:
    - from: "backend/evaluation/vlm_comparison.py"
      to: "openai.AsyncOpenAI"
      via: "OpenAIVisionProvider._get_client()"
      pattern: "AsyncOpenAI"
---

<objective>
Add OpenAIVisionProvider to vlm_comparison.py and refactor for multi-provider vision evaluation.

Purpose: EVAL-03 requires vlm_comparison.py to run 2 models (Gemini + OpenAI) x 3 conditions comparison matrix. Currently hardcodes GeminiVisionProvider.
Output: OpenAIVisionProvider class, refactored vlm_comparison.py with --provider flag, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-model-evaluation-comparison/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/evaluation/vlm_comparison.py:
```python
class GeminiVisionProvider:
    def __init__(self, api_key: str | None = None, model: str = "gemini-2.0-flash"): ...
    def is_available(self) -> bool: ...
    async def generate(self, system_prompt: str, user_prompt: str, images: list[bytes] | None = None) -> str: ...

async def run_condition(condition, analytics, markdown_context, image_bytes, vision_provider, judge_provider, analysis_type) -> dict: ...

# Current CLI: --analytics, --tracks, --video, --n-keyframes, --analysis-type, --output
# No --provider flag currently
```

From backend/evaluation/_common.py:
```python
@dataclass
class EvalConfig:
    provider: str = "gemini"
```

From backend/evaluation/llm_grounding.py (reused imports):
```python
from .llm_grounding import EXTRACTION_PROMPT, compute_grounding_score, extract_claims, format_as_markdown, verify_claim
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OpenAIVisionProvider + tests</name>
  <files>
    backend/evaluation/vlm_comparison.py,
    backend/evaluation/tests/test_openai_vision.py
  </files>
  <read_first>
    backend/evaluation/vlm_comparison.py,
    backend/evaluation/tests/conftest.py
  </read_first>
  <behavior>
    - Test 1: OpenAIVisionProvider.is_available() returns True when OPENAI_API_KEY is set, False when empty
    - Test 2: OpenAIVisionProvider.generate() without images sends a single text content part
    - Test 3: OpenAIVisionProvider.generate() with images sends text + image_url content parts with data:image/jpeg;base64,{b64} format
    - Test 4: OpenAIVisionProvider uses model "gpt-4o-mini" by default
  </behavior>
  <action>
    1. Create `backend/evaluation/tests/test_openai_vision.py` with unit tests for all behaviors above. Mock `openai.AsyncOpenAI` to avoid real API calls. Use `unittest.mock.patch.dict(os.environ, {"OPENAI_API_KEY": "test-key"})`. The mock's `chat.completions.create` should be an AsyncMock returning a response with `choices[0].message.content = "test vision response"`. For the images test, pass `images=[b"\xff\xd8\xff\xe0test"]` and assert the content parts list contains both a text part and an image_url part with the correct base64 data URL prefix `data:image/jpeg;base64,`.
    2. Add `OpenAIVisionProvider` class to `backend/evaluation/vlm_comparison.py` (place it after `GeminiVisionProvider`):
       - `__init__(self, api_key: str | None = None, model: str = "gpt-4o-mini")`
       - `self.api_key = api_key or os.getenv("OPENAI_API_KEY", "")`
       - `is_available()` returns `bool(self.api_key)`
       - `_get_client()` lazily creates `AsyncOpenAI(api_key=self.api_key)` from `openai`
       - `generate(self, system_prompt: str, user_prompt: str, images: list[bytes] | None = None) -> str`:
         - Build `content_parts = [{"type": "text", "text": user_prompt}]`
         - If images: for each `img_bytes`, `b64 = base64.b64encode(img_bytes).decode("utf-8")`, append `{"type": "image_url", "image_url": {"url": f"data:image/jpeg;base64,{b64}"}}`
         - Call `client.chat.completions.create(model=self.model_name, messages=[{"role": "system", "content": system_prompt}, {"role": "user", "content": content_parts}], temperature=0.7, max_tokens=4096)`
         - Return `response.choices[0].message.content or ""`
    3. Add `import base64` at top of vlm_comparison.py (after existing imports).
  </action>
  <verify>
    <automated>cd /Users/eshansharif/Documents/football-analytics-dashboard/backend && python -m pytest evaluation/tests/test_openai_vision.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/evaluation/vlm_comparison.py contains `class OpenAIVisionProvider:`
    - backend/evaluation/vlm_comparison.py contains `data:image/jpeg;base64,`
    - backend/evaluation/vlm_comparison.py contains `gpt-4o-mini`
    - backend/evaluation/vlm_comparison.py contains `import base64`
    - backend/evaluation/tests/test_openai_vision.py contains `test_generate_with_images`
    - backend/evaluation/tests/test_openai_vision.py contains `test_is_available`
    - All tests in test_openai_vision.py pass (exit code 0)
  </acceptance_criteria>
  <done>OpenAIVisionProvider class in vlm_comparison.py, all 4 unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Refactor vlm_comparison.py for multi-provider --provider flag</name>
  <files>
    backend/evaluation/vlm_comparison.py
  </files>
  <read_first>
    backend/evaluation/vlm_comparison.py
  </read_first>
  <action>
    1. Add a `VISION_PROVIDERS` registry dict at module level (after the provider classes):
       ```python
       VISION_PROVIDERS = {
           "gemini": GeminiVisionProvider,
           "openai": OpenAIVisionProvider,
       }
       ```
    2. In `main()` argparse, add `--provider` argument:
       `parser.add_argument("--provider", default="gemini", choices=["gemini", "openai", "all"])`
    3. Refactor `run_async()` to accept a `provider` parameter (add it to the function signature):
       - Change signature: `async def run_async(analytics_path, tracks_path, video_path, output_dir, n_keyframes=5, analysis_type="match_overview", provider="gemini")`
       - If `provider == "all"`, set `providers_to_run = list(VISION_PROVIDERS.keys())`; otherwise `providers_to_run = [provider]`
       - Wrap the existing conditions loop in an outer `for provider_name in providers_to_run:` loop
       - Create provider instance: `vision_provider = VISION_PROVIDERS[provider_name]()`
       - Check `if not vision_provider.is_available(): print(f"  Skipping {provider_name} (no API key)"); continue`
       - Use `get_provider(provider_name)` for judge_provider (with fallback to any available)
       - Prefix output keys with provider_name: `results[f"{provider_name}_{condition}"] = result`
       - Save per-provider vlm_results JSON: `f"vlm_results_{provider_name}.json"`
       - Generate per-provider LaTeX table with provider name in caption
       - Generate per-provider bar chart with provider name in filename/title
    4. After the provider loop, if `len(providers_to_run) > 1`, add a cross-provider comparison table:
       - For each condition, show grounding_rate per provider, saved as `vlm_provider_comparison.tex`
    5. Update `main()` to pass `args.provider` to `run_async()`.
    6. Update `run()` sync wrapper to accept and pass `provider` parameter.
    7. Keep the file under 500 lines.
  </action>
  <verify>
    <automated>cd /Users/eshansharif/Documents/football-analytics-dashboard/backend && python -c "from evaluation.vlm_comparison import VISION_PROVIDERS, main; print(f'Providers: {list(VISION_PROVIDERS.keys())}'); import argparse; p = argparse.ArgumentParser(); p.add_argument('--provider', choices=['gemini','openai','all']); p.parse_args(['--provider','all']); print('CLI accepts --provider all')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/evaluation/vlm_comparison.py contains `VISION_PROVIDERS`
    - backend/evaluation/vlm_comparison.py contains `"openai": OpenAIVisionProvider`
    - backend/evaluation/vlm_comparison.py contains `choices=["gemini", "openai", "all"]`
    - backend/evaluation/vlm_comparison.py contains `providers_to_run`
    - backend/evaluation/vlm_comparison.py contains `vlm_provider_comparison`
    - File is under 500 lines (wc -l < 500)
  </acceptance_criteria>
  <done>vlm_comparison.py accepts --provider all, iterates Gemini + OpenAI vision providers, generates per-provider and cross-provider outputs</done>
</task>

</tasks>

<verification>
1. `cd backend && python -m pytest evaluation/tests/test_openai_vision.py -x -v` — all tests pass
2. `cd backend && python -c "from evaluation.vlm_comparison import OpenAIVisionProvider, VISION_PROVIDERS; print(VISION_PROVIDERS)"` — shows both providers
3. `wc -l backend/evaluation/vlm_comparison.py` — under 500 lines
</verification>

<success_criteria>
- OpenAIVisionProvider class sends base64 images via content parts
- vlm_comparison.py CLI accepts --provider gemini, --provider openai, --provider all
- Multi-provider loop skips unavailable providers gracefully
- Cross-provider comparison table generated when running --provider all
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-multi-model-evaluation-comparison/14-02-SUMMARY.md`
</output>
