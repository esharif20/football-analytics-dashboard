---
phase: 14-multi-model-evaluation-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/requirements.txt
  - backend/api/services/llm_providers.py
  - backend/evaluation/tests/__init__.py
  - backend/evaluation/tests/conftest.py
  - backend/evaluation/tests/test_hf_provider.py
  - backend/evaluation/llm_grounding.py
autonomous: true
requirements: [EVAL-01, EVAL-02]
must_haves:
  truths:
    - "Evaluation framework can query HuggingFace Inference API via Mistral-7B-Instruct for grounding analysis"
    - "Researcher can select huggingface as a provider when running grounding evaluation"
    - "Researcher can run grounding evaluation across all providers in a single command"
    - "Unavailable providers are skipped gracefully with a clear message"
  artifacts:
    - path: "backend/api/services/llm_providers.py"
      provides: "HuggingFaceProvider class"
      contains: "class HuggingFaceProvider(LLMProvider)"
    - path: "backend/evaluation/llm_grounding.py"
      provides: "Multi-provider CLI support"
      contains: "--provider all"
    - path: "backend/evaluation/tests/test_hf_provider.py"
      provides: "Unit tests for HuggingFaceProvider"
      contains: "test_generate_returns_text"
  key_links:
    - from: "backend/evaluation/llm_grounding.py"
      to: "backend/api/services/llm_providers.py"
      via: "get_provider('huggingface')"
      pattern: "get_provider.*huggingface"
---

<objective>
Add HuggingFaceProvider to the LLM provider abstraction and wire it into llm_grounding.py with multi-provider support.

Purpose: EVAL-01 requires a HuggingFace provider (Mistral-7B-Instruct via InferenceClient). EVAL-02 requires llm_grounding.py to run across multiple models (gemini + openai + huggingface).
Output: HuggingFaceProvider class in llm_providers.py, updated llm_grounding.py CLI with --provider all, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-model-evaluation-comparison/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/api/services/llm_providers.py:
```python
class LLMProvider(ABC):
    @abstractmethod
    async def generate(self, system_prompt: str, user_prompt: str) -> str: ...
    @abstractmethod
    def is_available(self) -> bool: ...

class OpenAIProvider(LLMProvider):
    def __init__(self, api_key: str | None = None, model: str = "gpt-4o-mini"): ...

def get_provider(provider_name: str | None = None) -> LLMProvider:
    # Registry dict: {"gemini": GeminiProvider, "openai": OpenAIProvider, "stub": StubProvider}
```

From backend/evaluation/llm_grounding.py:
```python
# Current CLI: --provider choices=["gemini", "openai", "stub"]
# EvalConfig has provider: str field
# run_async(config) calls get_provider(config.provider)
```

From backend/evaluation/_common.py:
```python
@dataclass
class EvalConfig:
    analytics_path: str = ""
    tracks_path: str = ""
    homography_path: str = ""
    video_path: str = ""
    annotations_path: str = ""
    output_dir: str = "eval_output"
    provider: str = "gemini"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add HuggingFaceProvider + tests + dependency</name>
  <files>
    backend/api/requirements.txt,
    backend/api/services/llm_providers.py,
    backend/evaluation/tests/__init__.py,
    backend/evaluation/tests/conftest.py,
    backend/evaluation/tests/test_hf_provider.py
  </files>
  <read_first>
    backend/api/services/llm_providers.py,
    backend/api/requirements.txt,
    backend/api/tests/conftest.py
  </read_first>
  <behavior>
    - Test 1: HuggingFaceProvider.is_available() returns True when api_key is set, False when empty
    - Test 2: HuggingFaceProvider.generate() calls AsyncInferenceClient.chat.completions.create() with correct model, messages, temperature, max_tokens and returns response.choices[0].message.content
    - Test 3: get_provider("huggingface") returns HuggingFaceProvider instance when HUGGINGFACE_API_KEY is set
    - Test 4: HuggingFaceProvider uses model "mistralai/Mistral-7B-Instruct-v0.3" by default
  </behavior>
  <action>
    1. Add `huggingface-hub>=1.0.0` to backend/api/requirements.txt (after the openai line).
    2. Run `pip install "huggingface-hub>=1.0.0"`.
    3. Create `backend/evaluation/tests/__init__.py` (empty file).
    4. Create `backend/evaluation/tests/conftest.py` with a mock_hf_client fixture that patches `huggingface_hub.AsyncInferenceClient` and returns a mock whose `chat.completions.create` is an AsyncMock returning a response object with `choices[0].message.content = "test response"`.
    5. Create `backend/evaluation/tests/test_hf_provider.py` with tests for all behaviors above. Use `unittest.mock.patch.dict(os.environ, {"HUGGINGFACE_API_KEY": "test-key"})` for env var tests. Mock `AsyncInferenceClient` to avoid real API calls.
    6. Add `HuggingFaceProvider` class to `backend/api/services/llm_providers.py` following the existing `OpenAIProvider` pattern:
       - `__init__(self, api_key: str | None = None, model: str = "mistralai/Mistral-7B-Instruct-v0.3")`
       - `self.api_key = api_key or os.getenv("HUGGINGFACE_API_KEY", "")`
       - `is_available()` returns `bool(self.api_key)`
       - `_get_client()` lazily creates `AsyncInferenceClient(api_key=self.api_key)` from `huggingface_hub`
       - `generate()` calls `client.chat.completions.create(model=self.model_name, messages=[{"role": "system", "content": system_prompt}, {"role": "user", "content": user_prompt}], temperature=0.7, max_tokens=4096)` and returns `response.choices[0].message.content or ""`
    7. Add `"huggingface": HuggingFaceProvider` to the `providers` dict in `get_provider()`.
    8. Update the RuntimeError message in `get_provider()` to include `HUGGINGFACE_API_KEY`.
  </action>
  <verify>
    <automated>cd /Users/eshansharif/Documents/football-analytics-dashboard/backend && python -m pytest evaluation/tests/test_hf_provider.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/api/services/llm_providers.py contains `class HuggingFaceProvider(LLMProvider):`
    - backend/api/services/llm_providers.py contains `"huggingface": HuggingFaceProvider`
    - backend/api/services/llm_providers.py contains `AsyncInferenceClient`
    - backend/api/services/llm_providers.py contains `mistralai/Mistral-7B-Instruct-v0.3`
    - backend/api/requirements.txt contains `huggingface-hub>=1.0.0`
    - backend/evaluation/tests/test_hf_provider.py contains `test_generate_returns_text`
    - backend/evaluation/tests/test_hf_provider.py contains `test_is_available`
    - All tests in test_hf_provider.py pass (exit code 0)
  </acceptance_criteria>
  <done>HuggingFaceProvider registered in get_provider(), all 4 unit tests pass, huggingface-hub in requirements.txt</done>
</task>

<task type="auto">
  <name>Task 2: Wire multi-provider --provider all into llm_grounding.py</name>
  <files>
    backend/evaluation/llm_grounding.py
  </files>
  <read_first>
    backend/evaluation/llm_grounding.py,
    backend/api/services/llm_providers.py
  </read_first>
  <action>
    1. In `main()` argparse, update `--provider` choices to `["gemini", "openai", "huggingface", "all", "stub"]`.
    2. In `run_async(config)`, after `provider = get_provider(config.provider)`, add multi-provider logic:
       - If `config.provider == "all"`, set `providers_to_run = ["gemini", "openai", "huggingface"]`
       - Otherwise `providers_to_run = [config.provider]`
       - Wrap the existing format x analysis_type loop in an outer `for provider_name in providers_to_run:` loop
       - Call `get_provider(provider_name)` for each; if `not provider.is_available()`, print `f"  Skipping {provider_name} (no API key)"` and `continue`
       - Prefix output artifact filenames and dict keys with provider_name: e.g. `f"{provider_name}_{fmt_name}_{analysis_type}"`
       - Generate per-provider LaTeX tables and figures (reuse existing `_plot_format_comparison` and `_plot_grounding_summary` with provider_name in title/filename)
    3. Update the `format_type_scores` structure to be `dict[str, dict[str, dict[str, dict]]]` keyed by `provider_name -> format -> analysis_type -> score`.
    4. At the end, add a cross-provider comparison table: for each analysis_type, show grounding_rate per provider (markdown format only), saved as `grounding_provider_comparison.tex`.
    5. Keep the file under 500 lines. The multi-provider loop should replace the existing single-provider loop, not duplicate it.
  </action>
  <verify>
    <automated>cd /Users/eshansharif/Documents/football-analytics-dashboard/backend && python -c "from evaluation.llm_grounding import main; import argparse; p = argparse.ArgumentParser(); p.add_argument('--provider', choices=['gemini','openai','huggingface','all','stub']); p.parse_args(['--provider','all']); print('CLI accepts --provider all')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/evaluation/llm_grounding.py contains `choices=["gemini", "openai", "huggingface", "all", "stub"]`
    - backend/evaluation/llm_grounding.py contains `providers_to_run`
    - backend/evaluation/llm_grounding.py contains `Skipping.*no API key`
    - backend/evaluation/llm_grounding.py contains `grounding_provider_comparison`
    - File is under 500 lines (wc -l < 500)
  </acceptance_criteria>
  <done>llm_grounding.py accepts --provider huggingface and --provider all; multi-provider loop iterates available providers and generates per-provider + cross-provider outputs</done>
</task>

</tasks>

<verification>
1. `cd backend && python -m pytest evaluation/tests/test_hf_provider.py -x -v` — all tests pass
2. `cd backend && python -c "from api.services.llm_providers import get_provider, HuggingFaceProvider; print('OK')"` — import succeeds
3. `cd backend && python -c "from evaluation.llm_grounding import main; print('OK')"` — import succeeds
4. `wc -l backend/api/services/llm_providers.py` — under 200 lines
5. `wc -l backend/evaluation/llm_grounding.py` — under 500 lines
</verification>

<success_criteria>
- HuggingFaceProvider class exists and follows LLMProvider ABC
- get_provider("huggingface") works
- llm_grounding.py CLI accepts --provider huggingface and --provider all
- Multi-provider loop skips unavailable providers gracefully
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-multi-model-evaluation-comparison/14-01-SUMMARY.md`
</output>
