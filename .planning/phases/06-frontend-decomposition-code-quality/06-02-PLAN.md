---
phase: 06-frontend-decomposition-code-quality
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/pages/Analysis.tsx
  - frontend/src/pages/analysis/index.tsx
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - "Analysis page renders identically after decomposition"
    - "Analysis.tsx (or analysis/index.tsx) is under 400 lines"
    - "All sub-components are imported from pages/analysis/components/"
  artifacts:
    - path: "frontend/src/pages/analysis/index.tsx"
      provides: "Page shell with routing, top-level state, layout grid, imports from components/"
      min_lines: 100
      exports: ["default"]
    - path: "frontend/src/pages/Analysis.tsx"
      provides: "Re-export from pages/analysis/index.tsx for backwards compatibility"
      min_lines: 1
  key_links:
    - from: "frontend/src/pages/analysis/index.tsx"
      to: "frontend/src/pages/analysis/components/*.tsx"
      via: "named imports from ./components/*"
      pattern: "from ['\"]\\./components/"
    - from: "frontend/src/pages/analysis/index.tsx"
      to: "frontend/src/pages/analysis/context.ts"
      via: "import { TeamColorsCtx, ... } from './context'"
      pattern: "from ['\"]\\./context['\"]"
---

<objective>
Replace the 2358-line Analysis.tsx monolith with a slim page shell (~300-400 lines) that imports sub-components from Plan 01's extracted files. The old Analysis.tsx becomes a re-export shim.

Purpose: Complete QUAL-01 — Analysis.tsx under 400 lines with sub-components in pages/analysis/.
Output: Slim analysis/index.tsx as new page entry, old Analysis.tsx re-exports it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-decomposition-code-quality/06-CONTEXT.md
@.planning/phases/06-frontend-decomposition-code-quality/06-01-SUMMARY.md
@frontend/src/pages/Analysis.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create slim analysis/index.tsx page shell</name>
  <files>frontend/src/pages/analysis/index.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (full file in sections — focus on the main `export default function Analysis()` at line 129 through its return JSX, identifying which sub-components are used where)
    - frontend/src/pages/analysis/context.ts (created by Plan 01)
    - frontend/src/pages/analysis/components/VideoPlayer.tsx (created by Plan 01)
    - frontend/src/pages/analysis/components/StatsPanel.tsx (created by Plan 01)
    - frontend/src/pages/analysis/components/ChartsGrid.tsx (created by Plan 01)
    - frontend/src/pages/analysis/components/PitchVisualizations.tsx (created by Plan 01)
    - frontend/src/pages/analysis/components/AICommentary.tsx (created by Plan 01)
    - frontend/src/pages/analysis/components/PlayerStats.tsx (created by Plan 01)
    - frontend/src/pages/analysis/components/EventTimeline.tsx (created by Plan 01)
    - frontend/src/pages/analysis/components/PipelineInfo.tsx (created by Plan 01)
  </read_first>
  <action>
Create `frontend/src/pages/analysis/index.tsx` containing ONLY:

1. **Imports** — React hooks, react-query, wouter, api-local, lucide-react icons, date-fns, useWebSocket, useAuth, and imports from `./context` and `./components/*`.

2. **The main `Analysis()` function** (currently lines 129-842 in Analysis.tsx) — this is the page shell containing:
   - `useParams`, `useQuery` calls for analysis/events/statistics data
   - `useWebSocket` hook connection
   - `useState` for local UI state (activeTab, selectedPlayer, etc.)
   - `useMemo` computations for derived data (demoEvents, passNetwork, teamStats, etc.)
   - `useEffect` hooks for WS message handling, refetch logic
   - TeamColorsCtx.Provider wrapping the JSX layout
   - The return JSX layout grid that renders the extracted sub-components

3. **Replace inline component references with imports:**
   - `<VideoPlayer analysis={analysis} videoUrl={videoUrl} />` instead of inline video JSX
   - `<StatsPanel ...props />` or use individual exports: `<QuickStat>`, `<StatusBadge>`, `<ProcessingStatus>`, `<StatRow>`
   - `<PossessionDonut>`, `<TeamPerformanceRadar>`, etc. from `./components/ChartsGrid`
   - `<PitchRadar>`, `<ModeSpecificTabs>`, etc. from `./components/PitchVisualizations`
   - `<AICommentarySection>` from `./components/AICommentary`
   - `<PlayerStatsTable>` from `./components/PlayerStats`
   - `<EventTimeline>` from `./components/EventTimeline`
   - `<PipelinePerformanceCard>`, `<ComingSoonCard>`, `<BallTrajectoryDiagram>`, `<PlayerInteractionGraph>` from `./components/PipelineInfo`

4. **Do NOT include** any component function definitions that were extracted in Plan 01. Those now live in components/*.tsx.

5. **Export default:** `export default function Analysis() { ... }`

The file MUST be under 400 lines. The main Analysis() function body (hooks + JSX layout) should be approximately 300-400 lines since it retains all the data-fetching, state management, and layout composition logic.

CRITICAL: The JSX layout structure must remain IDENTICAL to the original. Same CSS classes, same element nesting, same conditional rendering. Only difference is that inline component definitions are replaced with imported component references. Props must be passed correctly to match each component's signature.
  </action>
  <verify>
    <automated>wc -l frontend/src/pages/analysis/index.tsx | awk '{if ($1 <= 400) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (max 400)"}'</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/analysis/index.tsx exists
    - File is under 400 lines (`wc -l` output <= 400)
    - File contains `export default function Analysis()`
    - File contains `from "./context"` import
    - File contains `from "./components/VideoPlayer"` import
    - File contains `from "./components/ChartsGrid"` import
    - File contains `from "./components/PitchVisualizations"` import
    - File contains `from "./components/AICommentary"` import
    - File contains `from "./components/PlayerStats"` import
    - File contains `from "./components/EventTimeline"` import
    - File contains `from "./components/PipelineInfo"` import
    - File contains `from "./components/StatsPanel"` import
    - File does NOT contain `function PossessionDonut(` (extracted)
    - File does NOT contain `function PitchRadar(` (extracted)
    - File does NOT contain `function AICommentarySection(` (extracted)
    - File does NOT contain `function PlayerStatsTable(` (extracted)
    - File contains `TeamColorsCtx.Provider` (still wraps the layout)
  </acceptance_criteria>
  <done>analysis/index.tsx is the slim page shell under 400 lines, importing all sub-components</done>
</task>

<task type="auto">
  <name>Task 2: Replace old Analysis.tsx with re-export shim and verify build</name>
  <files>frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (current 2358-line file — about to be replaced)
    - frontend/src/pages/analysis/index.tsx (just created in Task 1)
  </read_first>
  <action>
1. Replace the entire contents of `frontend/src/pages/Analysis.tsx` with a single re-export:

```typescript
export { default } from "./analysis/index";
```

This preserves any existing import paths that reference `@/pages/Analysis` or `../pages/Analysis`.

2. Run the frontend build to verify no broken imports or type errors:

```bash
cd frontend && npx tsc --noEmit 2>&1 | head -50
```

If TypeScript errors appear, fix them. Common issues:
- Missing prop types on extracted components (add `any` temporarily if needed, matching original code style)
- Import paths off by one directory level (should be `./components/X` from analysis/index.tsx, `../context` from components/X.tsx)
- Circular imports (context.ts should not import from components)

3. Run the dev build check:

```bash
cd frontend && npx vite build 2>&1 | tail -20
```

If build fails, fix the specific import/export issues reported.
  </action>
  <verify>
    <automated>cd frontend && npx vite build 2>&1 | grep -q "built in" && echo "PASS: build succeeds" || echo "FAIL: build broken"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/Analysis.tsx contains exactly `export { default } from "./analysis/index"`
    - frontend/src/pages/Analysis.tsx is under 5 lines
    - `cd frontend && npx vite build` completes without errors
    - `cd frontend && npx tsc --noEmit` completes without errors (or only pre-existing warnings)
  </acceptance_criteria>
  <done>Old monolith replaced with re-export shim, frontend builds successfully with decomposed structure</done>
</task>

</tasks>

<verification>
- `wc -l frontend/src/pages/analysis/index.tsx` returns <= 400
- `wc -l frontend/src/pages/Analysis.tsx` returns <= 5
- `cd frontend && npx vite build` succeeds
- `ls frontend/src/pages/analysis/components/*.tsx | wc -l` returns 8
- `grep -c "from.*./components/" frontend/src/pages/analysis/index.tsx` returns >= 8
</verification>

<success_criteria>
- Analysis page shell (analysis/index.tsx) is under 400 lines
- Old Analysis.tsx is a re-export shim
- Frontend builds without errors
- All component imports resolve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-decomposition-code-quality/06-02-SUMMARY.md`
</output>
