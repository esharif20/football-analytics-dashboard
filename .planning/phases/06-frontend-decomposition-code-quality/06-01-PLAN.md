---
phase: 06-frontend-decomposition-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/analysis/context.ts
  - frontend/src/pages/analysis/components/VideoPlayer.tsx
  - frontend/src/pages/analysis/components/StatsPanel.tsx
  - frontend/src/pages/analysis/components/ChartsGrid.tsx
  - frontend/src/pages/analysis/components/PitchVisualizations.tsx
  - frontend/src/pages/analysis/components/AICommentary.tsx
  - frontend/src/pages/analysis/components/PlayerStats.tsx
  - frontend/src/pages/analysis/components/EventTimeline.tsx
  - frontend/src/pages/analysis/components/PipelineInfo.tsx
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - "Analysis page sub-components exist in pages/analysis/components/ with correct exports"
    - "Shared context and types are importable from pages/analysis/context.ts"
  artifacts:
    - path: "frontend/src/pages/analysis/context.ts"
      provides: "TeamColorsCtx, useTeamColors, AnimatedSection, shared types, constants"
      exports: ["TeamColorsCtx", "useTeamColors", "AnimatedSection", "PITCH_WIDTH", "PITCH_HEIGHT", "TEAM1_DEFAULT", "TEAM2_DEFAULT"]
    - path: "frontend/src/pages/analysis/components/VideoPlayer.tsx"
      provides: "Video playback section extracted from Analysis.tsx lines ~490-560"
    - path: "frontend/src/pages/analysis/components/StatsPanel.tsx"
      provides: "QuickStat, StatRow, StatusBadge, ProcessingStatus, possession stats"
    - path: "frontend/src/pages/analysis/components/ChartsGrid.tsx"
      provides: "PossessionDonut, TeamPerformanceRadar, StatsComparisonBar, TeamShapeChart, DefensiveLineChart, PressingIntensityChart"
    - path: "frontend/src/pages/analysis/components/PitchVisualizations.tsx"
      provides: "PitchRadar, PlayerNode, HeatmapView, PassNetworkView, VoronoiView, ModeSpecificTabs"
    - path: "frontend/src/pages/analysis/components/AICommentary.tsx"
      provides: "AICommentarySection, EmptyCommentaryState"
    - path: "frontend/src/pages/analysis/components/PlayerStats.tsx"
      provides: "PlayerStatsTable"
    - path: "frontend/src/pages/analysis/components/EventTimeline.tsx"
      provides: "EventTimeline"
    - path: "frontend/src/pages/analysis/components/PipelineInfo.tsx"
      provides: "PipelinePerformanceCard, ComingSoonCard, BallTrajectoryDiagram, PlayerInteractionGraph"
  key_links:
    - from: "frontend/src/pages/analysis/components/*.tsx"
      to: "frontend/src/pages/analysis/context.ts"
      via: "import { useTeamColors, AnimatedSection, ... } from '../context'"
      pattern: "from ['\"]\\.\\.(/|/\\.\\./)context['\"]"
---

<objective>
Extract all sub-components from Analysis.tsx into separate files under pages/analysis/components/, and create the shared context file. This plan creates the component files only — the next plan rewires Analysis.tsx to import them.

Purpose: Prepare all extracted component files so Plan 02 can slim down Analysis.tsx to under 400 lines by importing these modules.
Output: 9 new files (1 context + 8 component files) ready to be imported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-decomposition-code-quality/06-CONTEXT.md
@frontend/src/pages/Analysis.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared context, types, and constants file</name>
  <files>frontend/src/pages/analysis/context.ts</files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (lines 1-130 for imports, constants, TeamColorsCtx, useTeamColors, AnimatedSection, ChartTooltip)
  </read_first>
  <action>
Create `frontend/src/pages/analysis/context.ts` with these exact exports extracted from Analysis.tsx:

1. **Constants** (lines 82-86):
```typescript
export const PITCH_WIDTH = 105;
export const PITCH_HEIGHT = 68;
export const TEAM1_DEFAULT = "#e05252";
export const TEAM2_DEFAULT = "#4a9ede";
```

2. **TeamColorsCtx** (line 88):
```typescript
export const TeamColorsCtx = createContext({ TEAM1_HEX: TEAM1_DEFAULT, TEAM2_HEX: TEAM2_DEFAULT });
export function useTeamColors() { return useContext(TeamColorsCtx); }
```

3. **ChartTooltip** (lines 92-105) — small helper per D-02, stays shared since multiple chart components use it:
```typescript
export function ChartTooltip({ active, payload, label }: any) { ... }
```
Copy the full implementation from Analysis.tsx lines 92-105.

4. **AnimatedSection** (lines 107-127) — per D-04, shared since used across multiple sections:
```typescript
export function AnimatedSection({ children, className = "", delay = 0 }: { children: React.ReactNode; className?: string; delay?: number }) { ... }
```
Copy the full IntersectionObserver implementation from Analysis.tsx lines 107-127.

5. **Re-export shared types** used across components:
```typescript
export type { PipelineMode } from "@/shared/types";
export { PIPELINE_MODES, PROCESSING_STAGES, EVENT_TYPES } from "@/shared/types";
```

Imports needed: `createContext`, `useContext`, `useState`, `useEffect`, `useRef` from "react".
  </action>
  <verify>
    <automated>test -f frontend/src/pages/analysis/context.ts && grep -c "export" frontend/src/pages/analysis/context.ts | grep -q "^[5-9]\|^[1-9][0-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/analysis/context.ts exists
    - File contains `export const PITCH_WIDTH = 105`
    - File contains `export const PITCH_HEIGHT = 68`
    - File contains `export const TEAM1_DEFAULT = "#e05252"`
    - File contains `export const TEAM2_DEFAULT = "#4a9ede"`
    - File contains `export const TeamColorsCtx = createContext`
    - File contains `export function useTeamColors()`
    - File contains `export function ChartTooltip(`
    - File contains `export function AnimatedSection(`
    - File contains `export { PIPELINE_MODES, PROCESSING_STAGES, EVENT_TYPES }`
  </acceptance_criteria>
  <done>context.ts exists with all shared constants, context, helpers, and type re-exports</done>
</task>

<task type="auto">
  <name>Task 2: Extract all 8 component files from Analysis.tsx</name>
  <files>
    frontend/src/pages/analysis/components/VideoPlayer.tsx
    frontend/src/pages/analysis/components/StatsPanel.tsx
    frontend/src/pages/analysis/components/ChartsGrid.tsx
    frontend/src/pages/analysis/components/PitchVisualizations.tsx
    frontend/src/pages/analysis/components/AICommentary.tsx
    frontend/src/pages/analysis/components/PlayerStats.tsx
    frontend/src/pages/analysis/components/EventTimeline.tsx
    frontend/src/pages/analysis/components/PipelineInfo.tsx
  </files>
  <read_first>
    - frontend/src/pages/Analysis.tsx (full file — read in sections: 1-130, 130-500, 500-850, 844-1000, 997-1375, 1373-1520, 1518-1800, 1788-2000, 2000-2358)
    - frontend/src/pages/analysis/context.ts (just created in Task 1 — import shared items from here)
  </read_first>
  <action>
Extract components per D-01 decisions. Each file must import shared items from `../context` (not re-define them). Each file must have its own React and library imports.

**File 1: `VideoPlayer.tsx`**
- Extract the video player JSX section from the main `Analysis()` component (approximately lines 490-560 where the `<video>` element and its controls are rendered)
- Export a single component: `export function VideoPlayer({ analysis, videoUrl }: { analysis: any; videoUrl: string | null })`
- The component receives the analysis object and computed videoUrl as props
- Import from `../context`: AnimatedSection

**File 2: `StatsPanel.tsx`**
- Extract: `QuickStat` (line 1846), `StatusBadge` (line 1870), `StatRow` (line 2333), `ProcessingStatus` (line 1887)
- Per D-02, these are small helpers — group them together since they form the stats display section
- Export all four: `export function QuickStat(...)`, `export function StatusBadge(...)`, `export function StatRow(...)`, `export function ProcessingStatus(...)`
- Import from `../context`: useTeamColors (if used), AnimatedSection

**File 3: `ChartsGrid.tsx`**
- Extract: `PossessionDonut` (line 1518), `TeamPerformanceRadar` (line 1564), `StatsComparisonBar` (line 1597), `TeamShapeChart` (line 1649), `DefensiveLineChart` (line 1697), `PressingIntensityChart` (line 1737)
- Export all six chart components
- Import from `../context`: useTeamColors, ChartTooltip, AnimatedSection
- Import from "recharts": all chart primitives used by these components

**File 4: `PitchVisualizations.tsx`**
- Extract: `PitchRadar` (line 2003), `PlayerNode` (line 2063), `HeatmapView` (line 2080), `PassNetworkView` (line 2119), `VoronoiView` (line 2171), `ModeSpecificTabs` (line 2285)
- Export all six components
- Import from `../context`: useTeamColors, PITCH_WIDTH, PITCH_HEIGHT, TEAM1_DEFAULT, TEAM2_DEFAULT, AnimatedSection

**File 5: `AICommentary.tsx`**
- Extract: `AICommentarySection` (line 844), `EmptyCommentaryState` (line 954)
- Export both components
- Import from `../context`: AnimatedSection
- Import Streamdown from "streamdown", useMutation/useQueryClient from "@tanstack/react-query", commentaryApi from "@/lib/api-local"

**File 6: `PlayerStats.tsx`**
- Extract: `PlayerStatsTable` (line 1373)
- Export: `export function PlayerStatsTable(...)`
- Import from `../context`: useTeamColors, AnimatedSection

**File 7: `EventTimeline.tsx`**
- Extract: `EventTimeline` (line 2250)
- Export: `export function EventTimeline(...)`
- Import from `../context`: EVENT_TYPES, AnimatedSection

**File 8: `PipelineInfo.tsx`**
- Extract: `PipelinePerformanceCard` (line 1788), `ComingSoonCard` (line 1468), `BallTrajectoryDiagram` (line 997), `PlayerInteractionGraph` (line 1093) plus its interfaces (InteractionNode, InteractionEdge, TimelineSegment, InteractionGraphData at lines 1088-1091)
- Export all four components and the interfaces
- Import from `../context`: useTeamColors, PITCH_WIDTH, PITCH_HEIGHT, AnimatedSection

CRITICAL: Copy each component's full implementation verbatim from Analysis.tsx. Do NOT simplify, rewrite, or "improve" the code. The goal is mechanical extraction with zero behavior change.

For each file, add the necessary React imports and library imports that the component uses (check the original imports at lines 1-79 of Analysis.tsx and only include what each file needs).
  </action>
  <verify>
    <automated>ls frontend/src/pages/analysis/components/*.tsx | wc -l | grep -q "8" && echo "PASS: 8 component files" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - 8 files exist in frontend/src/pages/analysis/components/
    - VideoPlayer.tsx contains `export function VideoPlayer(`
    - StatsPanel.tsx contains `export function QuickStat(` AND `export function StatusBadge(` AND `export function StatRow(` AND `export function ProcessingStatus(`
    - ChartsGrid.tsx contains `export function PossessionDonut(` AND `export function TeamPerformanceRadar(` AND `export function StatsComparisonBar(`
    - PitchVisualizations.tsx contains `export function PitchRadar(` AND `export function PlayerNode(` AND `export function ModeSpecificTabs(`
    - AICommentary.tsx contains `export function AICommentarySection(` AND `export function EmptyCommentaryState(`
    - PlayerStats.tsx contains `export function PlayerStatsTable(`
    - EventTimeline.tsx contains `export function EventTimeline(`
    - PipelineInfo.tsx contains `export function PipelinePerformanceCard(` AND `export function BallTrajectoryDiagram(` AND `export function PlayerInteractionGraph(`
    - Every component file imports from `../context` (grep confirms)
    - No file re-declares PITCH_WIDTH, TEAM1_DEFAULT, TeamColorsCtx, or AnimatedSection
  </acceptance_criteria>
  <done>All 8 component files extracted with correct imports, exports, and verbatim implementations</done>
</task>

</tasks>

<verification>
- `ls frontend/src/pages/analysis/components/*.tsx | wc -l` returns 8
- `test -f frontend/src/pages/analysis/context.ts` succeeds
- `grep -l "from.*context" frontend/src/pages/analysis/components/*.tsx | wc -l` returns 8 (all import from context)
- `grep -rn "PITCH_WIDTH = 105" frontend/src/pages/analysis/` returns only context.ts (no duplication)
</verification>

<success_criteria>
- 9 new files created (1 context + 8 components)
- Each component file exports the correct named functions
- All shared constants/context/helpers live only in context.ts
- Original Analysis.tsx is NOT modified yet (that is Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-decomposition-code-quality/06-01-SUMMARY.md`
</output>
