---
phase: 04-manus-dependency-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Home.tsx
  - frontend/src/_core/hooks/useAuth.ts
  - frontend/public/images/stadium.svg
  - frontend/public/images/heatmap.svg
  - frontend/public/images/ai-sports.svg
  - backend/.env.example
  - frontend/.env.example
autonomous: true
requirements: [MANUS-02, MANUS-03, MANUS-04]

must_haves:
  truths:
    - "Home page hero images load from local assets, not manuscdn.com"
    - "Auth hook uses 'football-dashboard-user' localStorage key"
    - "backend/.env.example exists with all required env vars documented"
    - "frontend/.env.example exists with VITE_ env vars documented"
  artifacts:
    - path: "frontend/src/pages/Home.tsx"
      provides: "Local image references for hero section"
      contains: "/images/"
    - path: "frontend/src/_core/hooks/useAuth.ts"
      provides: "Renamed localStorage key"
      contains: "football-dashboard-user"
    - path: "backend/.env.example"
      provides: "Backend env var documentation"
      contains: "MODEL_URL_PLAYER"
    - path: "frontend/.env.example"
      provides: "Frontend env var documentation"
      contains: "VITE_OAUTH_PORTAL_URL"
  key_links:
    - from: "frontend/src/pages/Home.tsx"
      to: "frontend/public/images/"
      via: "static asset paths"
      pattern: "/images/"
    - from: "frontend/src/_core/hooks/useAuth.ts"
      to: "localStorage"
      via: "localStorage.setItem key"
      pattern: "football-dashboard-user"
---

<objective>
Replace Manus CDN hero images with local placeholders, rename the auth localStorage key, and create .env.example files for both backend and frontend.

Purpose: Eliminate frontend Manus dependencies and document all environment variables for new developers.
Output: Zero manuscdn.com references in frontend; both .env.example files exist with full documentation.
</objective>

<execution_context>
@/Users/eshansharif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eshansharif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-manus-dependency-removal/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hero images with local SVG placeholders and rename localStorage key</name>
  <files>frontend/src/pages/Home.tsx, frontend/src/_core/hooks/useAuth.ts, frontend/public/images/stadium.svg, frontend/public/images/heatmap.svg, frontend/public/images/ai-sports.svg</files>
  <read_first>
    - frontend/src/pages/Home.tsx (lines 27-32: IMAGES const with manuscdn URLs)
    - frontend/src/_core/hooks/useAuth.ts (line 43: "manus-runtime-user-info" key)
  </read_first>
  <action>
**Part A: Create local placeholder images (per D-04)**

Create directory `frontend/public/images/` and add 3 SVG placeholder images:

1. `frontend/public/images/stadium.svg` — A simple SVG (800x400) with a dark green gradient background and white text "Stadium View". Use a rectangle with rounded corners, gradient fill from #1a472a to #2d5a3f, and centered white text.

2. `frontend/public/images/heatmap.svg` — A simple SVG (800x400) with a warm gradient background (orange-red) and white text "Heatmap Analysis". Use gradient from #e65100 to #ff8f00.

3. `frontend/public/images/ai-sports.svg` — A simple SVG (800x400) with a blue-purple gradient and white text "AI Sports Analytics". Use gradient from #1a237e to #4a148c.

Each SVG should be self-contained, roughly 15-25 lines, and visually appropriate as hero section imagery.

**Part B: Update Home.tsx image references (per D-04)**

In `frontend/src/pages/Home.tsx`, replace the IMAGES const (lines 28-32) from:

```typescript
const IMAGES = {
  stadium: "https://files.manuscdn.com/...",
  heatmap: "https://files.manuscdn.com/...",
  aiSports: "https://files.manuscdn.com/...",
};
```

To:

```typescript
const IMAGES = {
  stadium: "/images/stadium.svg",
  heatmap: "/images/heatmap.svg",
  aiSports: "/images/ai-sports.svg",
};
```

**Part C: Rename localStorage key (per D-05)**

In `frontend/src/_core/hooks/useAuth.ts`, on line 43, change:

```typescript
"manus-runtime-user-info",
```

To:

```typescript
"football-dashboard-user",
```

Ensure there are ZERO remaining references to `manus` anywhere in the frontend/src/ directory.
  </action>
  <verify>
    <automated>cd /Users/eshansharif/Documents/football-analytics-dashboard && grep -rc "manuscdn\|manus-runtime" frontend/src/ | grep -v ":0$" | wc -l | grep -q "^0$" && test -f frontend/public/images/stadium.svg && test -f frontend/public/images/heatmap.svg && test -f frontend/public/images/ai-sports.svg && grep -q "football-dashboard-user" frontend/src/_core/hooks/useAuth.ts && grep -q "/images/stadium.svg" frontend/src/pages/Home.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "manuscdn" frontend/src/` returns zero matches
    - `grep -r "manus-runtime" frontend/src/` returns zero matches
    - `frontend/public/images/stadium.svg` exists and contains `<svg`
    - `frontend/public/images/heatmap.svg` exists and contains `<svg`
    - `frontend/public/images/ai-sports.svg` exists and contains `<svg`
    - `grep "/images/stadium.svg" frontend/src/pages/Home.tsx` returns a match
    - `grep "/images/heatmap.svg" frontend/src/pages/Home.tsx` returns a match
    - `grep "/images/ai-sports.svg" frontend/src/pages/Home.tsx` returns a match
    - `grep "football-dashboard-user" frontend/src/_core/hooks/useAuth.ts` returns a match
    - `grep "manus-runtime-user-info" frontend/src/_core/hooks/useAuth.ts` returns zero matches
  </acceptance_criteria>
  <done>Home page loads images from local SVG placeholders. Auth hook uses "football-dashboard-user" localStorage key. Zero manus references in frontend/src/.</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example files for backend and frontend</name>
  <files>backend/.env.example, frontend/.env.example</files>
  <read_first>
    - backend/api/config.py (Settings class — all env vars with defaults)
    - backend/pipeline/worker.py (first 50 lines — DASHBOARD_URL, POLL_INTERVAL, MODEL_URL_* vars)
    - frontend/src/const.ts (VITE_* env vars)
  </read_first>
  <action>
Per D-06, create `backend/.env.example` with this exact content:

```
# Football Analytics Dashboard — Backend Environment Variables
# Copy to backend/.env and fill in values

# --- Database ---
# MySQL connection string (Phase 5 will migrate to PostgreSQL/Supabase)
DATABASE_URL=mysql+aiomysql://root:password@localhost:3307/football_analytics

# --- Authentication ---
# JWT signing secret — MUST change in production
JWT_SECRET=dev-secret-change-in-production

# --- CORS ---
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# --- Development Mode ---
# Set to "true" to enable auto-login middleware (dev only)
LOCAL_DEV_MODE=true
OWNER_OPEN_ID=local-dev-user

# --- Local Storage ---
LOCAL_STORAGE_DIR=./uploads

# --- Worker API ---
WORKER_API_KEY=

# --- ML Model URLs ---
# Download URLs for YOLO model weights (required for pipeline worker)
# These must point to .pt files accessible via HTTP GET
MODEL_URL_PLAYER=
MODEL_URL_BALL=
MODEL_URL_PITCH=

# --- Worker Configuration ---
# URL the worker polls for pending analyses
DASHBOARD_URL=http://localhost:8000
POLL_INTERVAL=5
COMPLETE_RETRIES=3

# --- LLM Tactical Commentary ---
# Provider: "gemini" or "openai"
LLM_PROVIDER=gemini
GEMINI_API_KEY=
OPENAI_API_KEY=
LLM_MODEL=
```

Per D-07, create `frontend/.env.example` with this exact content:

```
# Football Analytics Dashboard — Frontend Environment Variables
# Copy to frontend/.env and fill in values

# --- OAuth (optional) ---
# Leave empty for local dev mode (auto-login enabled)
# Set to your OAuth portal URL for production authentication
VITE_OAUTH_PORTAL_URL=
VITE_APP_ID=
```
  </action>
  <verify>
    <automated>cd /Users/eshansharif/Documents/football-analytics-dashboard && test -f backend/.env.example && test -f frontend/.env.example && grep -q "MODEL_URL_PLAYER" backend/.env.example && grep -q "MODEL_URL_BALL" backend/.env.example && grep -q "MODEL_URL_PITCH" backend/.env.example && grep -q "CORS_ORIGINS" backend/.env.example && grep -q "VITE_OAUTH_PORTAL_URL" frontend/.env.example && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/.env.example` exists and is non-empty
    - `frontend/.env.example` exists and is non-empty
    - `grep "MODEL_URL_PLAYER" backend/.env.example` returns a match
    - `grep "MODEL_URL_BALL" backend/.env.example` returns a match
    - `grep "MODEL_URL_PITCH" backend/.env.example` returns a match
    - `grep "CORS_ORIGINS" backend/.env.example` returns a match
    - `grep "JWT_SECRET" backend/.env.example` returns a match
    - `grep "DATABASE_URL" backend/.env.example` returns a match
    - `grep "DASHBOARD_URL" backend/.env.example` returns a match
    - `grep "VITE_OAUTH_PORTAL_URL" frontend/.env.example` returns a match
    - `grep "VITE_APP_ID" frontend/.env.example` returns a match
    - Both files contain inline comments (lines starting with #)
  </acceptance_criteria>
  <done>backend/.env.example and frontend/.env.example exist with all env vars documented with inline comments. A developer cloning the repo can copy these and know what to configure.</done>
</task>

</tasks>

<verification>
- `grep -r "manuscdn" frontend/src/` returns zero matches
- `grep -r "manus-runtime" frontend/src/` returns zero matches
- All 3 SVG files exist in frontend/public/images/
- backend/.env.example contains MODEL_URL_*, CORS_ORIGINS, DATABASE_URL, JWT_SECRET
- frontend/.env.example contains VITE_OAUTH_PORTAL_URL, VITE_APP_ID
- `npx tsc --noEmit` in frontend/ shows no new type errors (if applicable)
</verification>

<success_criteria>
1. Home page hero images reference local SVGs, not manuscdn.com
2. Auth localStorage key is "football-dashboard-user"
3. backend/.env.example documents all required env vars
4. frontend/.env.example documents all VITE_* env vars
5. Zero manuscdn.com or manus-runtime references in frontend/src/
</success_criteria>

<output>
After completion, create `.planning/phases/04-manus-dependency-removal/04-02-SUMMARY.md`
</output>
