# Football Analytics Dashboard — Frontend Environment Variables
# Copy to frontend/.env and fill in values

# --- OAuth (optional) ---
# Leave empty for local dev mode (auto-login enabled)
# Set to your OAuth portal URL for production authentication
VITE_OAUTH_PORTAL_URL=
VITE_APP_ID=
